# Required (set once cloud is ready; for now DRY_RUN=1 so it's optional)
SERVER_ID=beelink-01
AGENT_KEY=replace_with_long_random_hex_or_base64
INGEST_URL=https://100.101.102.103/api/ingest  # your VM's Tailscale IP/URL

# Optional
MC_LOG=/mc/logs/latest.log
METRIC_INTERVAL=30
QUEUE_DIR=/app/queue
DRY_RUN=1
